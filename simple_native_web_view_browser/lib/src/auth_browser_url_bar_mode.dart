/// Режим отображения адресной строки браузера.
enum AuthBrowserUrlBarMode {
  /// Адресная строка скрыта.
  hidden,

  /// Адресная строка отображается и доступна для редактирования.
  editable,

  /// Адресная строка отображается, но доступна только для чтения.
  readOnly,
}
